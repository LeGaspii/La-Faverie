<div class="container">
    <p><%= @room.house.name %></p>
    <p><%= @room.name %></p>
  </div>
<div class="container m-2">
  <%= link_to "Edit this room", edit_room_path(@room), class: "" %>
  <%= link_to "Remove",
              room_path(@room),
              method: :delete,
              data: { confirm: "Are you sure?" } %>

</div>
