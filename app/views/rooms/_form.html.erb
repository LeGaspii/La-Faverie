<%= simple_form_for(@room) do |f| %>
  <div class="form-inputs">
    <%= f.input :name, input_html: { placeholder: "Nom de la chambre" } %>
  </div>
<div class="mt-2 form-actions text-white btn-gradient container text-center">
    <%= f.button :submit %>
  </div>
<% end %>
