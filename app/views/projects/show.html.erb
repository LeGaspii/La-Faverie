
<div class="shadow bg-white">
    <div class=" bg-size bg-cover bg-center"
         style="background-image: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('<%= cl_image_path @project.photo.key, width: 1000, height: 500, crop: :fill %>')">
        <div class="p-4 h-64 flex items-center text-white flex justify-between">
            <h1 class="mb-2 text-5xl font-black"><%= @project.name %></h1>
            <div class="flex items-end h-full">
              <p class="mb-2 text-1xl"> à terminer avant : <%= @project.deadline.strftime("%d %m %Y") %></p>
            </div>
        </div>
    </div>
    <div class="p-4">
          <p class="text-gray-600"><%= @project.rich_body  %></p>
          <div class="flex justify-around w-full">
            <div class="tasks-display p-2 border border-gray-500 w-2/5 m-">
              <div>
              <div>

                <% @tasks.each do |task| %>
                <li class="my-2">

                <span><%= task.name %></span>
                <%# <%= simple_form_for [ @project, task] do |f| %>
                  <!-- <div class="btn-box">
                    <%# <%= f.input :completed, input_html: { class: 'toggle'   %> %>
                  </div> -->
                <%# <% end %>
                <% end %>
              </li>
              <div class="text-center">
              <%= link_to "Ajouter une tâche",
              new_project_task_path(@project),
              class: "uppercase px-8 py-2 rounded-full bg-blue-300 text-blue-600 max-w-max shadow-sm hover:shadow-lg" %>
            </div>
            </div>
              </div>
          </div>
          <div class="tasks-display p-2 border border-gray-500 w-2/5">
            <button class="uppercase px-8 py-2 rounded-full bg-blue-300 text-blue-600 max-w-max shadow-sm hover:shadow-lg">Ajouter un achat</button>

          </div>
        </div>
        <div class="mt-4 flex justify-between">
            <%= link_to "Modifier le projet", edit_project_path(@project), class: "mr-4 text-blue-500" %>
            <%= link_to "Effacer",
                        project_path(@project),
                        method: :delete,
                        data: { confirm: "Are you sure?" },
                        class: "mr-4 text-red-400" %>
        </div>
    </div>
</div>
